{
  "name": "numtick",
  "version": "1.0.0",
  "description": "NumTick - 자동 슈팅 서바이벌 게임",
  "main": "index.js",
  "scripts": {
    "build:www": "rm -rf www && mkdir -p www && cp index.html www/ && cp -r css www/ && cp -r js www/",
    "sync:android": "npm run build:www && npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug",
    "sync:ios": "npm run build:www && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hq1111302-glitch/numtick.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hq1111302-glitch/numtick/issues"
  },
  "homepage": "https://github.com/hq1111302-glitch/numtick#readme",
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0"
  }
}
